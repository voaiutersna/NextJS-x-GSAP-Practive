Workshop 3: การ Animate หลายชิ้นพร้อมกัน (stagger)
🎯 สิ่งที่จะเรียนรู้

ใช้ stagger เพื่อทำ animation หลาย element ซ้ำ ๆ

เข้าใจว่า stagger จะหน่วงเวลาอัตโนมัติ

🧠 ตัวอย่างสอน
"use client";
import { useRef, useEffect } from "react";
import { gsap } from "gsap";

export default function StaggerExample() {
  const listRef = useRef(null);

  useEffect(() => {
    const items = listRef.current.querySelectorAll("li");
    gsap.from(items, {
      y: 20,
      opacity: 0,
      duration: 0.6,
      ease: "power2.out",
      stagger: 0.15, // ← ไล่กันทีละชิ้น
    });
  }, []);

  return (
    <ul ref={listRef} className="text-white text-2xl space-y-2 p-10 bg-gray-900 min-h-screen">
      <li>Home</li>
      <li>Work</li>
      <li>About</li>
      <li>Contact</li>
    </ul>
  );
}

🧩 โจทย์ฝึก (Workshop 3)

สร้าง <ul> ที่มี 5 รายการ:
“HTML”, “CSS”, “JavaScript”, “React”, “GSAP”

ให้ใช้ stagger เพื่อให้มันค่อย ๆ เด้งขึ้นมาทีละตัว
ใส่ ease: "back.out(2)"
และเมื่อจบ animation ให้มัน bounce เล็กน้อย (yoyo: true, repeat: 1)

-------------------------------------------------------------------------

ตัวช่วยสำคัญที่ใช้คู่กับ repeat/yoyo

repeatDelay: s → หน่วงเวลา ระหว่างรอบ (ก่อนเริ่มรอบถัดไป)

gsap.to(".box", { x: 200, duration: 1, repeat: 2, yoyo: true, repeatDelay: 0.3 });


yoyoEase: true | "easeName" → เวลาย้อนกลับให้ “กลับ ease” ให้สมจริง
(ปกติการย้อนกลับจะใช้ ease เดิม ทำให้ผิวสัมผัสตอนกลับดูไม่เหมือนกัน)

gsap.to(".box", {
  x: 200,
  duration: 1,
  repeat: 1,
  yoyo: true,
  ease: "power3.out",
  yoyoEase: true // หรือกำหนด "power3.inOut"
});


stagger + yoyo → yoyo มีผลกับ แต่ละ target แยกกัน (ไม่ใช่ทั้งชุดพร้อมกัน)
ถ้าต้องการควบคุมทั้งชุด ใช้ timeline จะชัดกว่า

timeline({ repeat, yoyo }) → มีผลกับ ทั้งลำดับ
แต่สามารถ override รายตัวในแต่ละ tween ได้

จุดที่มือใหม่มักงง

“ทำไม yoyo: true แล้วไม่เห็นย้อน?”
→ ลืมตั้ง repeat ให้มากกว่า 0

“อยากให้หยุดที่ ‘ต้นทาง’ หลังเล่นไป–กลับ”
→ ใช้ repeat: 1, yoyo: true (ไปหนึ่งรอบ + ย้อนหนึ่งรอบ)

“เวลาย้อนกลับ ease ดูแปลก”
→ ใส่ yoyoEase: true หรือระบุชื่อ ease ที่เหมาะ

“อยากหน่วงก่อนเริ่มรอบใหม่”
→ ใช้ repeatDelay

// เต้นไม่หยุดแบบไป–กลับ พร้อม ease กลับที่สมจริง
gsap.to(".e", {
  scale: 1.1,
  duration: 0.6,
  repeat: -1,
  yoyo: true,
  ease: "power2.out",
  yoyoEase: true,
  repeatDelay: 0.1
});