🧩 Workshop 1: การเคลื่อนที่พื้นฐาน (Move & Fade)
🎯 สิ่งที่จะเรียนรู้

เข้าใจ gsap.to() และ gsap.from()

ใช้ useRef + useEffect เพื่อเริ่มแอนิเมชันเมื่อ render แล้ว

🧠 ตัวอย่างสอน
"use client";
import { useRef, useEffect } from "react";
import { gsap } from "gsap";

export default function BoxFade() {
  const box = useRef(null);

  useEffect(() => {
    // ทำให้กล่องเลื่อนจากซ้ายไปขวา
    gsap.to(box.current, {
      x: 200, // เคลื่อนในแนว x
      duration: 1.5,
      ease: "power2.out",
    });
  }, []);

  return (
    <div className="h-screen flex justify-center items-center bg-gray-900">
      <div
        ref={box}
        className="w-24 h-24 bg-green-400 rounded-xl shadow-lg"
      ></div>
    </div>
  );
}

🧩 โจทย์ฝึก (Workshop 1)

สร้างกล่อง 3 สี (แดง เขียว น้ำเงิน) แล้วให้พวกมัน “เคลื่อนที่ + จางเข้า”

ใช้ gsap.from()

ให้แต่ละกล่องเริ่มช้ากัน 0.2 วินาที

ใช้ ease: "back.out(1.7)"

ตัวอย่างผลลัพธ์ที่อยากให้ได้
🔹 กล่องแดงโผล่ขึ้นก่อน → เขียว → น้ำเงิน (แต่ละกล่องเลื่อนขึ้น + fade in)